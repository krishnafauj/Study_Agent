// Constants for the dashboard feature module.

export const API_URL = process.env.NEXT_PUBLIC_API_URL;

// How often to refresh section parse status while any section is still parsing.
export const PARSE_POLL_MS = 4000;

// Skeleton placeholder counts shown while the page loads.
export const SECTION_SKELETON_COUNT = 3;
export const CHAT_SKELETON_COUNT = 2;
